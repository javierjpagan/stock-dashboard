{
  "name": "polygon-proxy-server",
  "version": "1.0.0",
  "description": "Lightweight Express proxy for Polygon.io — keeps your API key server-side",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
